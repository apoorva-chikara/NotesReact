/**
 * URL constant are useful when you have multiple URLs to 
 * request the resources from the API
 */

export default class URLConstant {
    static baseURL = ' https://morning-tor-55292.herokuapp.com/';
}